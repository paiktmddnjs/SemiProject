<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.MemberMapper">

    <insert id="insertMember" parameterType="com.example.demo.dto.MemberDto" keyProperty="memberId">
        INSERT INTO MEMBER (
        MEMBER_ID, EMAIL, MEMBER_PWD, MEMBER_NAME, PHONE, ENROLL_DATE, MODIFY_DATE, STATUS, ROLE
        ) VALUES (
        SEQ_MEMBER_ID.NEXTVAL,  <!-- 시퀀스 사용 -->
        #{email, jdbcType=VARCHAR},
        #{memberPwd, jdbcType=VARCHAR},
        #{memberName, jdbcType=VARCHAR},
        #{phone, jdbcType=VARCHAR},
        SYSTIMESTAMP,
        SYSTIMESTAMP,
        #{status, jdbcType=VARCHAR},
        #{role, jdbcType=VARCHAR}
        )
    </insert>

</mapper>
